1. 统一变量命名规则，尤其布尔类型，并将规则记录文档，后续变量都应遵循。[官方编码标准](https://dev.epicgames.com/documentation/unreal-engine/epic-cplusplus-coding-standard-for-unreal-engine)
2. 继续解决玩家攻击漂移问题：目标是鼠标左键攻击时胶囊体（Capsule）真实向前小跨一步，攻击结束后停在新位置；避免完整 Root Motion 导致平移过远，也避免关闭 Root Motion 后动画弹回原位。下一轮优先尝试 `Set Anim Root Motion Translation Scale` 的 `0.2`、`0.3`、`0.4`，并在播放（Play）模式下开启 `show collision` 校对胶囊体位移。
