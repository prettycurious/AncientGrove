# 2026-06-04 开发记录

## 当天目标

完成 Unreal Engine 项目基础搭建，让工程能够打开主地图并使用自定义 GameMode / Player 结构继续开发。

## 完成内容

1. 创建 Unreal Engine 蓝图项目 `AncientGrove`；当前项目文件关联版本为 UE 5.8；
2. 配置项目基础信息和默认地图；
3. 创建主地图 `M_AncientGrove`；
4. 创建 `BP_AncientGroveGameMode`；
5. 设置项目默认 GameMode 指向 `BP_AncientGroveGameMode`；
6. 准备玩家角色蓝图 `BP_PlayerCharacter` 作为后续默认 Pawn；
7. 地图中保留 `PlayerStart`，用于玩家出生点测试；
8. 初步整理项目目录，建立 `Content/AncientGrove/` 下的地图、蓝图等资源结构。

## 关键资产

| 资产 | 用途 |
|---|---|
| `AncientGrove.uproject` | 项目入口文件 |
| `Config/DefaultEngine.ini` | 默认地图和 GameMode 配置 |
| `Content/AncientGrove/Maps/M_AncientGrove.umap` | 当前主地图 |
| `Content/AncientGrove/Blueprints/Game/BP_AncientGroveGameMode.uasset` | 当前游戏模式 |
| `Content/AncientGrove/Blueprints/Character/BP_PlayerCharacter.uasset` | 玩家角色蓝图 |

## 问题记录

1. 当前仍处于基础搭建阶段，尚未形成完整玩法闭环；
2. 后续需要优先验证角色移动、输入映射和攻击输入；
3. 文档需要随着实际蓝图进度持续更新，避免只保留规划内容。

## 下一步

1. 完善玩家角色移动参数；
2. 添加 Shift 跑步输入；
3. 添加鼠标左键攻击输入；
4. 建立第一版项目开发文档。
