# 2026-06-09 开发记录

## 当天目标

进入 v0.4 敌人 AI 阶段，重点实现敌人发现玩家、追击玩家和进入攻击范围后的攻击准备逻辑。

## 完成内容

1. 继续更新 `BP_PlayerCharacter`，补充玩家战斗和受击相关逻辑；
2. 继续更新 `BP_AncientGuardian`，加入敌人 AI 相关变量：
   - `DetectRange`
   - `AttackRange`
   - `AttackInterval`
   - `CanAttack`
   - `PlayerRef`
3. 敌人蓝图中加入玩家距离检测相关节点；
4. 敌人蓝图中加入 `AI Move To` 节点；
5. `AI Move To` 当前连接方式：
   - `Pawn = Self`
   - `Target Actor = PlayerRef`
   - `Acceptance Radius = AttackRange`
6. 主地图中已有 `BP_AncientGuardian` 敌人实例；
7. 主地图中已有 `NavMeshBoundsVolume`；
8. 主地图中已有 `RecastNavMesh`；
9. 调整过 `NavMeshBoundsVolume` 和敌人实例相关关卡外部 Actor 资产。

## 关键资产

| 资产 | 用途 |
|---|---|
| `Content/AncientGrove/Blueprints/Enemy/BP_AncientGuardian.uasset` | 敌人 AI、追击、攻击准备逻辑 |
| `Content/AncientGrove/Blueprints/Character/BP_PlayerCharacter.uasset` | 玩家战斗和受击逻辑 |
| `Content/__ExternalActors__/AncientGrove/Maps/M_AncientGrove/7/LD/9FZOUYDQMRG651WCVFMU2W.uasset` | `NavMeshBoundsVolume` 关卡实例 |
| `Content/__ExternalActors__/AncientGrove/Maps/M_AncientGrove/E/CD/TBHW67BR9EWX6EX2V9U02O.uasset` | `BP_AncientGuardian` 关卡实例 |

## 问题记录

1. 敌人不追击玩家；
2. 初步排查发现 `AI Move To` 蓝图连接本身没有明显错误；
3. 地图中有 `NavMeshBoundsVolume` 和 `RecastNavMesh`，但需要确认绿色 NavMesh 是否覆盖敌人脚下；
4. 如果敌人脚下没有绿色导航区域，`AI Move To` 会被调用但无法实际移动；
5. 需要重点检查地面碰撞和导航影响设置。

## 下一步

1. 在编辑器视口按 `P` 显示绿色 NavMesh；
2. 调整 `NavMeshBoundsVolume`，让绿色区域覆盖敌人脚下和玩家到敌人的路径；
3. 执行 构建（Build）-> 构建路径（Build Paths）；
4. 播放（Play）测试敌人是否能追击；
5. 如果仍不追击，继续检查 `PlayerRef` 是否在 `BeginPlay` 中稳定赋值。
