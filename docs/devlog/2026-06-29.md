# 2026-06-29 开发日志

## 今日目标

今天继续推进 v0.6 打斗精修，完成 3 段攻击挥击音效的临时素材生成、导入接入和测试。

本次目标不是替换已完成的命中音效，而是补充另一类音效：

```text
Swing Sound / 挥击音效
```

挥击音效和命中音效的区别：

| 类型 | 播放时机 | 当前状态 |
|---|---|---|
| Hit Sound / 命中音效 | 攻击真正命中 Boss 后播放 | 已完成并测试通过 |
| Swing Sound / 挥击音效 | 攻击挥出去时播放，空挥也播放 | 本日已接入并测试通过 |

## 一、当前已有命中音效复查

### 1. 目的：避免重复做已经完成的命中音效

通过截图和 Unreal MCP 复查，当前 `BP_PlayerCharacter.AttackHitCheck` 中已经根据 `AttackIndex` 选择 3 段不同命中音效：

```text
AttackIndex == 1 -> Attack1HitSound
AttackIndex == 2 -> Attack2HitSound
其他情况       -> Attack3HitSound
```

MCP 读取到的当前命中音效变量：

| 变量 | 当前资源 |
|---|---|
| `Attack1HitSound` | `/Game/AncientGrove/Audio/SFX_Hit_LightPunch_01` |
| `Attack2HitSound` | `/Game/AncientGrove/Audio/SFX_Hit_HeavyPunch_01` |
| `Attack3HitSound` | `/Game/AncientGrove/Audio/SFX_Hit_Kick_01` |

校对结论：

```text
3 段不同命中音效已经完成。
下一步做的是挥击音效，不是重复做命中音效。
```

## 二、临时挥击音效生成

### 1. 目的：为 3 段攻击准备空挥也能播放的临时空气声

已生成 3 个临时 `.wav` 源文件：

| 文件 | 用途 | 时长 | 格式 |
|---|---|---:|---|
| `SFX_Swing_LightPunch_01.wav` | 第一段轻拳挥击 | 0.180 秒 | 44.1 kHz / 16-bit / Mono |
| `SFX_Swing_HeavyPunch_01.wav` | 第二段重拳挥击 | 0.240 秒 | 44.1 kHz / 16-bit / Mono |
| `SFX_Swing_Kick_01.wav` | 第三段腿击挥击 | 0.300 秒 | 44.1 kHz / 16-bit / Mono |

源文件目录：

```text
SourceAssets/Audio/
```

当前完整源文件包括：

```text
SFX_Hit_LightPunch_01.wav
SFX_Hit_HeavyPunch_01.wav
SFX_Hit_Kick_01.wav
SFX_Swing_LightPunch_01.wav
SFX_Swing_HeavyPunch_01.wav
SFX_Swing_Kick_01.wav
```

说明：

```text
这些挥击音效是程序合成的临时测试素材。
声音设计偏空气挥击，不加入明显撞击感，避免和命中音效混淆。
后续可以替换为正式素材库音效。
```

## 三、挥击音效导入和 Montage 接入

### 1. 目的：记录 3 段攻击空挥音效的接入结果

已将 3 个挥击音效导入 Unreal Editor：

```text
Content/AncientGrove/Audio/
```

当前资源路径：

| 攻击段 | 挥击音效 |
|---|---|
| `Attack_1` | `/Game/AncientGrove/Audio/SFX_Swing_LightPunch_01` |
| `Attack_2` | `/Game/AncientGrove/Audio/SFX_Swing_HeavyPunch_01` |
| `Attack_3` | `/Game/AncientGrove/Audio/SFX_Swing_Kick_01` |

已在 `AM_Player_Attack_Combo` 中为 3 段攻击分别添加播放声音 Notify：

```text
SFX Notify / 播放声音 Notify
```

校对记录：

```text
Unreal MCP 已确认：
- `/Game/AncientGrove/Audio` 下存在 3 个 `SFX_Swing_*` 资源；
- `AM_Player_Attack_Combo` 依赖中已包含这 3 个挥击音效；
- `AM_Player_Attack_Combo` 当前已保存。
```

说明：

```text
挥击音效放在 Montage Notify 里，因此空挥也会播放。
命中音效仍保留在 BP_PlayerCharacter.AttackHitCheck -> PlayHitFeedback 中，因此只有真正命中 Boss 才播放。
```

## 四、测试结果

测试标准：

| 测试项 | 预期结果 |
|---|---|
| 空挥 `Attack_1` | 只播放轻拳挥击音效，不播放命中音效 |
| 空挥 `Attack_2` | 只播放重拳挥击音效，不播放命中音效 |
| 空挥 `Attack_3` | 只播放腿击挥击音效，不播放命中音效 |
| 命中 Boss | 同时有挥击音效、命中音效、命中特效和镜头轻微抖动 |

当前测试结论：

```text
3 段挥击音效测试通过。
空挥和命中时机区分正常。
挥击音效与已完成的 3 段命中音效不是同一套逻辑，当前两者可以同时工作。
```

## 五、下一步建议

下一步建议进入攻击滑步和攻击期间移动手感：

```text
先检查攻击时是否可以边跑边挥拳；
再决定是先限制攻击期间移动，还是给攻击段增加轻微前移。
```
